# RTOS-Embedded-Audio
Real-time audio classification system using a Cortex-M3 RTOS node and a Linux host. WAV audio is analyzed, transformed (FFT), sent to the embedded system via UART, processed with RTOS tasks, and identified. 
